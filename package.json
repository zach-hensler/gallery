{
  "name": "gallery",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://zach-hensler.github.io/gallery",
  "scripts": {
    "dev": "vite",
    "test": "vitest --run",
    "build": "tsc && npm run test && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "preact": "^10.23.2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.1",
    "vitest": "^2.0.5"
  }
}
